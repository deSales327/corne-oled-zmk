name: Build ZMK firmware

on:
  workflow_dispatch:
  push:
    branches:
      - baseline/pandakb-corne
    paths:
      - "config/**"
      - "build.yaml"
      - ".github/workflows/build.yml"
  pull_request:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
