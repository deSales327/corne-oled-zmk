name: Build ZMK Recovery Firmware

on:
  push:
    branches:
      - recovery/last-known-good
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
